<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    
    <style name="myKeyboardStyleNumbers">
        <item name="android:keyTextColor">#fffcf5fc</item>
        <item name="android:keyBackground">@drawable/number_buttons</item>
        <item name="android:keyPreviewLayout">@layout/keyboard_preview</item>
        <item name="android:keyPreviewHeight">0dp</item>
    </style>

    <style name="myKeyboardStyleOperands">
        <item name="android:keyTextColor">#fffcf5fc</item>
        <item name="android:keyBackground">@drawable/operands_buttons</item>
        <item name="android:keyPreviewLayout">@layout/keyboard_preview</item>
        <item name="android:keyPreviewHeight">0dp</item>
    </style>

</resources>
